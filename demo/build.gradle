apply plugin: 'com.android.application'

android {
  compileSdkVersion 28

  defaultConfig {
    minSdkVersion 14
    targetSdkVersion 28
  }
}

dependencies {
  implementation project(':libraries:collage-views')
}
